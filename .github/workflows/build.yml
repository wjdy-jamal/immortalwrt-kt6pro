name: Test GitHub Actions

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check GitHub Actions
      run: |
        echo "🧪 اختبار GitHub Actions الأساسي"
        echo "✅ النظام: Ubuntu $(lsb_release -rs)"
        echo "📅 الوقت: $(date)"
        echo "🐧 النواة: $(uname -r)"
        
    - name: Check Disk Space
      run: |
        echo "💾 مساحة القرص المتوفرة:"
        df -h
        
    - name: Success
      run: |
        echo "🎉 الاختبار نجح!"
        echo "✅ GitHub Actions يعمل بشكل صحيح"
        echo "✅ يمكننا الآن متابعة بناء ImmortalWrt"
